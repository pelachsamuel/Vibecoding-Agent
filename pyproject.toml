[project]
name = "gemini-geometry-agent"
version = "0.1.0"
description = "A ReAct AI agent calculating distances using Google Gemini"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "google-genai>=0.12.0",
    "python-dotenv>=1.0.1",
]